# import * from './generated/prisma.graphql'

type Mutation {
  createUser(email: String, password: String, online: Boolean, status: Boolean, photo: String,
   name: String, jobTitle: String, telephone: String, nickname: String): User!
}

type Query {
  users: [User]!
}
